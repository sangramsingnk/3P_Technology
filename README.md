# Self-Learning Impact Analysis System (RAG + LLM) — Submission

**Candidate**: Sangramsing N. Kayte  
**Assignment used**: Technical Assignment — LLM & RAG Implementation. :contentReference[oaicite:1]{index=1}

## Deliverables
- Working prototype (code in `src/`)
- Knowledge base with 10 example projects (`data/example_projects.json`)
- Learning metrics dashboard (`src/dashboard/app.py`)
- Demo showing before/after learning (`src/demo/run_demo.py`)
- Strategy document and ROI computation (`docs/strategy_document.md`, `docs/roi_calculation.md`)
- 30-min presentation (`presentation/slides.md` + `presentation/create_pptx.py`)

## How to run (local)
1. Create virtualenv and install:
```bash
python -m venv venv
source venv/bin/activate
pip install -r requirements.txt
